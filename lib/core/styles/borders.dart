import 'package:flutter/material.dart';

class CustomBorders {
  static const radiusNone = 0.0;
  /** 
   * @returns 4.0
   * */
  static const radiusXS = 4.0;
  /** 
   * @returns 8.0
   * */
  static const radiusSM = 8.0;
  /** 
   * @returns 16.0
   * */
  static const radiusMD = 16.0;
  /** 
   * @returns 24.0
   * */
  static const radiusLG = 24.0;

  /** 
   * @returns 40.0
   * */
  static const radiusXL = 40.0;
  /** 
   * @returns BoxShape.circle
   * */
  static const radiusCircle = BoxShape.circle;
  static const Radius radiusFull = Radius.circular(100);

  /// @returns 0.0
  static const widthNone = 0.0;

  /// @returns 1.0
  static const widthSM = 1.0;

  /// @returns 2.0
  static const widthMD = 2.0;

  /// @returns 4.0
  static const widthLG = 4.0;
}
